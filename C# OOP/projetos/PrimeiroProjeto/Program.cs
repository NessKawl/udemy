using System; // referência para o namespace padrão System, é um namespace padrão do .NET (contém funções básicas de manipulação de programas)

namespace PrimeiroProjeto // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        static void Main(string[] args) // identificação padrão do C# para identificar o entry point da aplicação (onde a execução começa)
        {
            Console.WriteLine("Hello World!"); // o comando Console.WriteLine pertence ao namespace System, 
        }
    }
}